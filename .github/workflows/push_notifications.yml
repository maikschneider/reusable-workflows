name: test-build-release
on:
  push:
    branches:
      - main

jobs:

  release:
    runs-on: ubuntu-latest
    steps:
      - name: Notification
        uses: aliencube/microsoft-teams-actions@v0.8.0
        with:
          webhook_uri: ${{ secrets.TEAMS_WEBHOOK_URL }}
          title: 'New commit: "${{ github.event.push.message }}"'
          summary: 'New commit: "${{ github.event.push.message }}"'
          theme_color: "7ee787"
          sections: '[{ "activityTitle": "[${{ github.event.push.committer.name }}](${{ github.event.push.author.html_url }})", "activitySubtitle": "+${{ github.event.push.stats.addtions }} additions\n-${{ github.event.push.stats.addtions }} deletions", "activityImage": "${{ github.event.push.author.avatar_url }}"" }]'
          actions: '[{ "@type": "OpenUri", "name": "Show commit", "targets": [{ "os": "default", "uri": "${{ github.event.html_url }}" }] }]'