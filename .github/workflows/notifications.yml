name: notifications

on:
  workflow_call:
    inputs:
      image-url:
        description: 'Optional image URL to display in the card header (e.g. extension icon)'
        required: false
        type: string
        default: ''
    secrets:
      teams-webhook-url:
        required: true

jobs:

  microsoft_teams:
    name: Microsoft Teams Notifications
    runs-on: ubuntu-latest
    steps:

      - name: Process image URL
        id: image
        env:
          RAW_IMAGE_URL: ${{ inputs.image-url }}
        run: |
          # Strip query string/fragment for extension check
          url_path="${RAW_IMAGE_URL%%\?*}"
          url_path="${url_path%%#*}"
          if [[ "$url_path" == *.svg ]]; then
            echo "url=https://images.weserv.nl/?url=$(printf '%s' "$RAW_IMAGE_URL" | jq -sRr @uri)&output=png&w=64" >> "$GITHUB_OUTPUT"
          else
            echo "url=$RAW_IMAGE_URL" >> "$GITHUB_OUTPUT"
          fi

      - name: New release
        if: github.event_name == 'release' && github.event.action == 'published'
        env:
          WEBHOOK_URL: ${{ secrets.teams-webhook-url }}
          IMAGE_URL: ${{ steps.image.outputs.url }}
          REPO_NAME: ${{ github.repository }}
          REPO_URL: ${{ github.event.repository.html_url }}
          REPO_STARS: ${{ github.event.repository.stargazers_count }}
          REPO_FORKS: ${{ github.event.repository.forks_count }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          RELEASE_NAME: ${{ github.event.release.name }}
          RELEASE_BODY: ${{ github.event.release.body }}
          RELEASE_URL: ${{ github.event.release.html_url }}
          RELEASE_ZIP: ${{ github.event.release.zipball_url }}
          RELEASE_AUTHOR: ${{ github.event.release.author.login }}
          RELEASE_AUTHOR_URL: ${{ github.event.release.author.html_url }}
          RELEASE_AUTHOR_AVATAR: ${{ github.event.release.author.avatar_url }}
        run: |
          # Convert markdown headings (e.g. "## What's Changed") to bold so they
          # are not rendered as oversized headlines in the Teams card, then crop.
          body="$(printf '%s' "$RELEASE_BODY" | sed -E 's/^[[:space:]]*#{1,6}[[:space:]]+(.*)$/**\1**/')"
          if [ "${#body}" -gt 1500 ]; then
            # Truncate then drop any partial multi-byte char left at the boundary.
            body="$(printf '%s' "$body" | head -c 1500 | iconv -f UTF-8 -t UTF-8 -c || true)…"
          fi
          jq -n \
            --arg repo "$REPO_NAME" \
            --arg image "$IMAGE_URL" \
            --arg title "📦 New release: $RELEASE_TAG" \
            --arg name "$RELEASE_NAME" \
            --arg body "$body" \
            --arg url "$RELEASE_URL" \
            --arg zip "$RELEASE_ZIP" \
            --arg repo_url "$REPO_URL" \
            --arg stars "$REPO_STARS" \
            --arg forks "$REPO_FORKS" \
            --arg author "$RELEASE_AUTHOR" \
            --arg author_url "$RELEASE_AUTHOR_URL" \
            --arg avatar "$RELEASE_AUTHOR_AVATAR" \
            '{
              type: "message",
              summary: $title,
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                contentUrl: null,
                content: {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  type: "AdaptiveCard",
                  version: "1.4",
                  body: [
                    (if $image != "" then
                      { type: "ColumnSet", columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }] }
                      ]}
                    else
                      { type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }
                    end),
                    { type: "TextBlock", size: "medium", weight: "bolder", text: $title, color: "good" },
                    (if $image != "" then
                      {
                        type: "ColumnSet",
                        columns: [
                          { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                          { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: ("by [\($author)](\($author_url))"), weight: "bolder" }] }
                        ]
                      }
                    else
                      { type: "TextBlock", text: ("by [\($author)](\($author_url))"), weight: "bolder" }
                    end),
                    { type: "TextBlock", text: $name, weight: "bolder", wrap: true },
                    { type: "TextBlock", text: $body, wrap: true },
                    {
                      type: "FactSet",
                      facts: [
                        { title: "⭐ Stars", value: $stars },
                        { title: "🍴 Forks", value: $forks }
                      ]
                    }
                  ],
                  actions: [
                    { type: "Action.OpenUrl", title: "Show release", url: $url },
                    { type: "Action.OpenUrl", title: "Download zip", url: $zip },
                    { type: "Action.OpenUrl", title: "View repository", url: $repo_url }
                  ]
                }
              }]
            }' | curl -sSf -X POST -H "Content-Type: application/json" -d @- "$WEBHOOK_URL"

      - name: New issue
        if: github.event_name == 'issues' && github.event.action == 'opened'
        env:
          WEBHOOK_URL: ${{ secrets.teams-webhook-url }}
          IMAGE_URL: ${{ steps.image.outputs.url }}
          REPO_NAME: ${{ github.repository }}
          REPO_URL: ${{ github.event.repository.html_url }}
          REPO_STARS: ${{ github.event.repository.stargazers_count }}
          REPO_FORKS: ${{ github.event.repository.forks_count }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
          ISSUE_AUTHOR: ${{ github.event.issue.user.login }}
          ISSUE_AUTHOR_URL: ${{ github.event.issue.user.html_url }}
          ISSUE_AUTHOR_AVATAR: ${{ github.event.issue.user.avatar_url }}
          ISSUE_LABELS: ${{ join(github.event.issue.labels.*.name, ', ') }}
        run: |
          # Crop long issue bodies so they are not shown in full length.
          body="$ISSUE_BODY"
          if [ "${#body}" -gt 800 ]; then
            # Truncate then drop any partial multi-byte char left at the boundary.
            body="$(printf '%s' "$body" | head -c 800 | iconv -f UTF-8 -t UTF-8 -c || true)…"
          fi
          jq -n \
            --arg repo "$REPO_NAME" \
            --arg image "$IMAGE_URL" \
            --arg title "🔥 New issue #${ISSUE_NUMBER}: \"${ISSUE_TITLE}\"" \
            --arg body "$body" \
            --arg url "$ISSUE_URL" \
            --arg author "$ISSUE_AUTHOR" \
            --arg author_url "$ISSUE_AUTHOR_URL" \
            --arg avatar "$ISSUE_AUTHOR_AVATAR" \
            --arg repo_url "$REPO_URL" \
            --arg stars "$REPO_STARS" \
            --arg forks "$REPO_FORKS" \
            --arg labels "$ISSUE_LABELS" \
            '{
              type: "message",
              summary: $title,
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                contentUrl: null,
                content: {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  type: "AdaptiveCard",
                  version: "1.4",
                  body: [
                    (if $image != "" then
                      { type: "ColumnSet", columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }] }
                      ]}
                    else
                      { type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }
                    end),
                    { type: "TextBlock", size: "medium", weight: "bolder", text: $title, color: "attention" },
                    {
                      type: "ColumnSet",
                      columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $avatar, size: "small", style: "person" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: ("[\($author)](\($author_url))"), weight: "bolder" }] }
                      ]
                    },
                    { type: "TextBlock", text: $body, wrap: true },
                    {
                      type: "FactSet",
                      facts: ([
                        (if $labels != "" then { title: "🏷️ Labels", value: $labels } else null end),
                        { title: "⭐ Stars", value: $stars },
                        { title: "🍴 Forks", value: $forks }
                      ] | map(select(. != null)))
                    }
                  ],
                  actions: [
                    { type: "Action.OpenUrl", title: "View issue", url: $url },
                    { type: "Action.OpenUrl", title: "View repository", url: $repo_url }
                  ]
                }
              }]
            }' | curl -sSf -X POST -H "Content-Type: application/json" -d @- "$WEBHOOK_URL"

      - name: Issue closed
        if: github.event_name == 'issues' && github.event.action == 'closed'
        env:
          WEBHOOK_URL: ${{ secrets.teams-webhook-url }}
          IMAGE_URL: ${{ steps.image.outputs.url }}
          REPO_NAME: ${{ github.repository }}
          REPO_URL: ${{ github.event.repository.html_url }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
          CLOSED_BY: ${{ github.event.sender.login }}
          CLOSED_BY_URL: ${{ github.event.sender.html_url }}
          CLOSED_BY_AVATAR: ${{ github.event.sender.avatar_url }}
          ISSUE_STATE_REASON: ${{ github.event.issue.state_reason }}
        run: |
          jq -n \
            --arg repo "$REPO_NAME" \
            --arg image "$IMAGE_URL" \
            --arg title "🏁 Issue #${ISSUE_NUMBER} closed: \"${ISSUE_TITLE}\"" \
            --arg url "$ISSUE_URL" \
            --arg repo_url "$REPO_URL" \
            --arg closed_by "$CLOSED_BY" \
            --arg closed_by_url "$CLOSED_BY_URL" \
            --arg closed_by_avatar "$CLOSED_BY_AVATAR" \
            --arg state_reason "$ISSUE_STATE_REASON" \
            '{
              type: "message",
              summary: $title,
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                contentUrl: null,
                content: {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  type: "AdaptiveCard",
                  version: "1.4",
                  body: [
                    (if $image != "" then
                      { type: "ColumnSet", columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }] }
                      ]}
                    else
                      { type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }
                    end),
                    { type: "TextBlock", size: "medium", weight: "bolder", text: $title, color: "good" },
                    {
                      type: "ColumnSet",
                      columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $closed_by_avatar, size: "small", style: "person" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: ("Closed by [\($closed_by)](\($closed_by_url))"), weight: "bolder" }] }
                      ]
                    },
                    (if $state_reason != "" then
                      { type: "TextBlock", text: ("Reason: \($state_reason)"), size: "small", isSubtle: true, wrap: true }
                    else
                      null
                    end)
                  ] | map(select(. != null)),
                  actions: [
                    { type: "Action.OpenUrl", title: "View issue", url: $url },
                    { type: "Action.OpenUrl", title: "View repository", url: $repo_url }
                  ]
                }
              }]
            }' | curl -sSf -X POST -H "Content-Type: application/json" -d @- "$WEBHOOK_URL"

      - name: New issue comment
        if: github.event_name == 'issue_comment' && github.event.action == 'created' && github.actor != 'coderabbitai' && github.actor != 'codecov-commenter' && github.event.sender.type != 'Bot'
        env:
          WEBHOOK_URL: ${{ secrets.teams-webhook-url }}
          IMAGE_URL: ${{ steps.image.outputs.url }}
          REPO_NAME: ${{ github.repository }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          COMMENT_URL: ${{ github.event.comment.html_url }}
          COMMENT_AUTHOR: ${{ github.event.comment.user.login }}
          COMMENT_AUTHOR_URL: ${{ github.event.comment.user.html_url }}
          COMMENT_AUTHOR_AVATAR: ${{ github.event.comment.user.avatar_url }}
        run: |
          jq -n \
            --arg repo "$REPO_NAME" \
            --arg image "$IMAGE_URL" \
            --arg title "💬 New comment on issue #${ISSUE_NUMBER}: \"${ISSUE_TITLE}\"" \
            --arg body "$(echo "$COMMENT_BODY" | head -c 2000)" \
            --arg url "$COMMENT_URL" \
            --arg issue_url "$ISSUE_URL" \
            --arg author "$COMMENT_AUTHOR" \
            --arg author_url "$COMMENT_AUTHOR_URL" \
            --arg avatar "$COMMENT_AUTHOR_AVATAR" \
            '{
              type: "message",
              summary: $title,
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                contentUrl: null,
                content: {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  type: "AdaptiveCard",
                  version: "1.4",
                  body: [
                    (if $image != "" then
                      { type: "ColumnSet", columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }] }
                      ]}
                    else
                      { type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }
                    end),
                    { type: "TextBlock", size: "medium", weight: "bolder", text: $title, color: "accent" },
                    {
                      type: "ColumnSet",
                      columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $avatar, size: "small", style: "person" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: ("[\($author)](\($author_url))"), weight: "bolder" }] }
                      ]
                    },
                    { type: "TextBlock", text: $body, wrap: true }
                  ],
                  actions: [
                    { type: "Action.OpenUrl", title: "View comment", url: $url },
                    { type: "Action.OpenUrl", title: "View issue", url: $issue_url }
                  ]
                }
              }]
            }' | curl -sSf -X POST -H "Content-Type: application/json" -d @- "$WEBHOOK_URL"

      - name: New PR review comment
        if: github.event_name == 'pull_request_review_comment' && github.event.action == 'created' && github.actor != 'coderabbitai' && github.actor != 'codecov-commenter' && github.event.sender.type != 'Bot'
        env:
          WEBHOOK_URL: ${{ secrets.teams-webhook-url }}
          IMAGE_URL: ${{ steps.image.outputs.url }}
          REPO_NAME: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          COMMENT_URL: ${{ github.event.comment.html_url }}
          COMMENT_PATH: ${{ github.event.comment.path }}
          COMMENT_AUTHOR: ${{ github.event.comment.user.login }}
          COMMENT_AUTHOR_URL: ${{ github.event.comment.user.html_url }}
          COMMENT_AUTHOR_AVATAR: ${{ github.event.comment.user.avatar_url }}
        run: |
          jq -n \
            --arg repo "$REPO_NAME" \
            --arg image "$IMAGE_URL" \
            --arg title "💬 New review comment on PR #${PR_NUMBER}: \"${PR_TITLE}\"" \
            --arg body "$(echo "$COMMENT_BODY" | head -c 2000)" \
            --arg url "$COMMENT_URL" \
            --arg pr_url "$PR_URL" \
            --arg path "$COMMENT_PATH" \
            --arg author "$COMMENT_AUTHOR" \
            --arg author_url "$COMMENT_AUTHOR_URL" \
            --arg avatar "$COMMENT_AUTHOR_AVATAR" \
            '{
              type: "message",
              summary: $title,
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                contentUrl: null,
                content: {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  type: "AdaptiveCard",
                  version: "1.4",
                  body: [
                    (if $image != "" then
                      { type: "ColumnSet", columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }] }
                      ]}
                    else
                      { type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }
                    end),
                    { type: "TextBlock", size: "medium", weight: "bolder", text: $title, color: "accent" },
                    {
                      type: "ColumnSet",
                      columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $avatar, size: "small", style: "person" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: ("[\($author)](\($author_url))"), weight: "bolder" }] }
                      ]
                    },
                    (if $path != "" then
                      { type: "TextBlock", text: ("📄 \($path)"), size: "small", isSubtle: true, wrap: true }
                    else
                      null
                    end),
                    { type: "TextBlock", text: $body, wrap: true }
                  ] | map(select(. != null)),
                  actions: [
                    { type: "Action.OpenUrl", title: "View comment", url: $url },
                    { type: "Action.OpenUrl", title: "View pull request", url: $pr_url }
                  ]
                }
              }]
            }' | curl -sSf -X POST -H "Content-Type: application/json" -d @- "$WEBHOOK_URL"

      - name: New star
        if: github.event_name == 'watch' && github.event.action == 'started'
        env:
          WEBHOOK_URL: ${{ secrets.teams-webhook-url }}
          IMAGE_URL: ${{ steps.image.outputs.url }}
          REPO_NAME: ${{ github.repository }}
          ACTOR: ${{ github.actor }}
          ACTOR_URL: ${{ github.event.sender.html_url }}
          ACTOR_AVATAR: ${{ github.event.sender.avatar_url }}
          REPO_URL: ${{ github.event.repository.html_url }}
          REPO_STARS: ${{ github.event.repository.stargazers_count }}
          REPO_FORKS: ${{ github.event.repository.forks_count }}
          REPO_DESCRIPTION: ${{ github.event.repository.description }}
        run: |
          jq -n \
            --arg repo "$REPO_NAME" \
            --arg image "$IMAGE_URL" \
            --arg title "⭐️ New star on ${REPO_NAME} by ${ACTOR}!" \
            --arg actor "$ACTOR" \
            --arg actor_url "$ACTOR_URL" \
            --arg avatar "$ACTOR_AVATAR" \
            --arg repo_url "$REPO_URL" \
            --arg stars "$REPO_STARS" \
            --arg forks "$REPO_FORKS" \
            --arg description "$REPO_DESCRIPTION" \
            '{
              type: "message",
              summary: $title,
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                contentUrl: null,
                content: {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  type: "AdaptiveCard",
                  version: "1.4",
                  body: [
                    (if $image != "" then
                      { type: "ColumnSet", columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }] }
                      ]}
                    else
                      { type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }
                    end),
                    { type: "TextBlock", size: "medium", weight: "bolder", text: $title, color: "warning" },
                    {
                      type: "ColumnSet",
                      columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $avatar, size: "small", style: "person" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: ("[\($actor)](\($actor_url))"), weight: "bolder" }] }
                      ]
                    },
                    (if $description != "" then
                      { type: "TextBlock", text: $description, wrap: true, size: "small", isSubtle: true }
                    else
                      null
                    end),
                    {
                      type: "FactSet",
                      facts: [
                        { title: "⭐ Stars", value: $stars },
                        { title: "🍴 Forks", value: $forks }
                      ]
                    }
                  ] | map(select(. != null)),
                  actions: [
                    { type: "Action.OpenUrl", title: "View repository", url: $repo_url },
                    { type: "Action.OpenUrl", title: "View stargazer", url: $actor_url }
                  ]
                }
              }]
            }' | curl -sSf -X POST -H "Content-Type: application/json" -d @- "$WEBHOOK_URL"

      - name: New pull request
        if: github.event_name == 'pull_request_target' && github.event.action == 'opened' && github.actor != 'renovate' && github.event.sender.type != 'Bot'
        env:
          WEBHOOK_URL: ${{ secrets.teams-webhook-url }}
          IMAGE_URL: ${{ steps.image.outputs.url }}
          REPO_NAME: ${{ github.repository }}
          REPO_URL: ${{ github.event.repository.html_url }}
          REPO_STARS: ${{ github.event.repository.stargazers_count }}
          REPO_FORKS: ${{ github.event.repository.forks_count }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          PR_AUTHOR_URL: ${{ github.event.pull_request.user.html_url }}
          PR_AUTHOR_AVATAR: ${{ github.event.pull_request.user.avatar_url }}
          PR_COMMITS: ${{ github.event.pull_request.commits }}
          PR_CHANGED_FILES: ${{ github.event.pull_request.changed_files }}
          PR_ADDITIONS: ${{ github.event.pull_request.additions }}
          PR_DELETIONS: ${{ github.event.pull_request.deletions }}
          PR_HEAD: ${{ github.event.pull_request.head.ref }}
          PR_BASE: ${{ github.event.pull_request.base.ref }}
        run: |
          # Crop long pull request bodies so they are not shown in full length.
          body="$PR_BODY"
          if [ "${#body}" -gt 800 ]; then
            # Truncate then drop any partial multi-byte char left at the boundary.
            body="$(printf '%s' "$body" | head -c 800 | iconv -f UTF-8 -t UTF-8 -c || true)…"
          fi
          jq -n \
            --arg repo "$REPO_NAME" \
            --arg image "$IMAGE_URL" \
            --arg title "🛠️ New pull request #${PR_NUMBER}: \"${PR_TITLE}\"" \
            --arg body "$body" \
            --arg url "$PR_URL" \
            --arg repo_url "$REPO_URL" \
            --arg author "$PR_AUTHOR" \
            --arg author_url "$PR_AUTHOR_URL" \
            --arg avatar "$PR_AUTHOR_AVATAR" \
            --arg commits "$PR_COMMITS" \
            --arg files "$PR_CHANGED_FILES" \
            --arg additions "+${PR_ADDITIONS}" \
            --arg deletions "-${PR_DELETIONS}" \
            --arg branch "${PR_HEAD} → ${PR_BASE}" \
            --arg stars "$REPO_STARS" \
            --arg forks "$REPO_FORKS" \
            '{
              type: "message",
              summary: $title,
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                contentUrl: null,
                content: {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  type: "AdaptiveCard",
                  version: "1.4",
                  body: [
                    (if $image != "" then
                      { type: "ColumnSet", columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $image, size: "small" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }] }
                      ]}
                    else
                      { type: "TextBlock", text: $repo, weight: "bolder", size: "small", color: "accent" }
                    end),
                    { type: "TextBlock", size: "medium", weight: "bolder", text: $title, color: "attention" },
                    {
                      type: "ColumnSet",
                      columns: [
                        { type: "Column", width: "auto", items: [{ type: "Image", url: $avatar, size: "small", style: "person" }] },
                        { type: "Column", width: "stretch", verticalContentAlignment: "center", items: [{ type: "TextBlock", text: ("[\($author)](\($author_url))"), weight: "bolder" }] }
                      ]
                    },
                    { type: "TextBlock", text: $body, wrap: true },
                    {
                      type: "FactSet",
                      facts: [
                        { title: "🔀 Branch", value: $branch },
                        { title: "Commits", value: $commits },
                        { title: "File changes", value: $files },
                        { title: "Additions", value: $additions },
                        { title: "Deletions", value: $deletions },
                        { title: "⭐ Stars", value: $stars },
                        { title: "🍴 Forks", value: $forks }
                      ]
                    }
                  ],
                  actions: [
                    { type: "Action.OpenUrl", title: "View pull request", url: $url },
                    { type: "Action.OpenUrl", title: "View repository", url: $repo_url }
                  ]
                }
              }]
            }' | curl -sSf -X POST -H "Content-Type: application/json" -d @- "$WEBHOOK_URL"
